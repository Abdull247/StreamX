# Backend base URL that the frontend will call.
# Set this to wherever your StreamX API server is running.
# Defaults (when this var is unset) to http://localhost:3001
VITE_API_BASE_URL=http://localhost:3001
